# 小程序离线数据

此目录由 `scripts/build_miniprogram_data.py` 从 `data/site_dataset.json` 生成。

```powershell
python scripts/build_final_site.py
python scripts/build_miniprogram_data.py
```

请不要手工改动 `questions/*.js` 或 `loaders.js`。
